<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "SzynCom" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
<!-- nav bar -->
<div class="top-bar topbar-center-logo" id="topbar-center-logo">
  <div class="top-bar-left">
    <%= link_to root_path do%>
      <img src="https://placehold.it/100x39" alt="logo placeholder" />
    <% end %>
    </ul>
  </div>
  <div class="top-bar-center search-box">
  
    <%= form_tag(user_search_path, method: :get)  do %>
      <div class="input-group input-group-rounded">
        <%= search_field_tag :search, params[:search], placeholder: "Search user", class: "input-group-field", :required => 'required' %>
        <div class="input-group-button">
          <%= submit_tag 'Search', class: "button secondary" %>
        </div>
      </div>
    <% end %>

  </div>
  <div class="top-bar-right">
    <ul class="menu vertical medium-horizontal">
      <% if user_signed_in? %>
        <li><%= link_to current_user.name, edit_user_registration_path %></li>
        <li><%= link_to 'Log out', destroy_user_session_path, method: 'delete' %></li>
      <% else %>
        <li><%= link_to 'Sign in', new_user_session_path %></li>
        <li><%= link_to 'Sign up', new_user_registration_path %></li>
      <% end %>
    </ul>
  </div>
</div>
<!-- /medium and larger nav bar -->


    
    <%= yield %>

  </body>
</html>
